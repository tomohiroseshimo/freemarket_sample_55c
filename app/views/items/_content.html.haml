.contents__body__wrapper__list
  - @item.each do |i|
    = link_to "/show/#{i.id}"  do
      - i.id
      .content
        .content__image
          - if i.images.count > 0
            %span.content-price
              = "¥ #{i.price}"
           # = image_tag i.images.first.variant(resize: "188x188").processed
        .content__description
          = i.description
    
    
  
   
    
  

  